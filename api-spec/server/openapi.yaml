openapi: '3.0.2'
info:
  title: popwaifu API
  version: '0.0.1'
servers:
  - url: '/api'
  - url: 'https://popwaifu.click/api'
paths:
  /:
    get:
      operationId: root.get
      summary: for testing api server is online
      responses:
        '200':
          description: normal response
          content:
            'application/json':
              schema:
                type: string
                example: 'popwaifu API'
